import React from 'react'

const BodyHTML = ({name, age}) => {
  return (
    <div>
        <h1>name: {name}</h1>
        <h2>age: {age}</h2>
        {/* gender, batch, sem, year */}
    </div>
  )
}

export default BodyHTML;


